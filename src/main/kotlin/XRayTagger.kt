class XRayTagger {
    fun tagTest(scenario: String, featureFile: java.io.File) : Boolean {
        // TODO Parse Feature File looking for Scenario.
        // TODO Add tag atop (if line exist, append, if blank, newline).
        return true
    }
}